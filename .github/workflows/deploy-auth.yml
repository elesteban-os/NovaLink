name: Deploy Auth Service

on:
  push:
    branches: [ "main" ]
    paths:
      - 'services/auth/**'
      - '.github/workflows/deploy-auth.yml'
      - 'k8s/auth.yaml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'services/auth/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v2
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v4
      with:
        context: ./services/auth
        push: true
        tags: |
          ghcr.io/${{ github.repository_owner }}/auth-service:latest
          ghcr.io/${{ github.repository_owner }}/auth-service:${{ github.sha }}

    # ---------------------------------------------------------
    # Sección de Despliegue en Kubernetes (Continuous Deployment)
    # ---------------------------------------------------------
    # - name: Set up Kubeconfig
    #   uses: azure/k8s-set-context@v3
    #   with:
    #     kubeconfig: ${{ secrets.KUBECONFIG }}
    #
    # - name: Deploy to Kubernetes
    #   run: |
    #     # Actualizar la imagen en el deployment para forzar el reinicio
    #     kubectl set image deployment/auth-deployment auth=ghcr.io/${{ github.repository_owner }}/auth-service:${{ github.sha }}
    #     # Verificar que el rollout fue exitoso
    #     kubectl rollout status deployment/auth-deployment
